export default (mail: string): string => {
  return `Hello, ${mail},unfortunately your subscription payment has failed 
  please subscribe to any plan in 24 hours 
  otherwise all your current points would be erased`;
};
